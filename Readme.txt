# SimpleDB with CRUD and JOIN Support

A lightweight, file-based key–value database implemented in Python, featuring basic CRUD operations and support for simple JOIN functionality. This project is designed for educational purposes to help understand core database concepts, such as data persistence, querying, and joining related data.

---

## 🚀 Features

- **CRUD Operations**  
  - **CREATE (SET)**: Insert or update a record  
  - **READ (GET)**: Retrieve a record  
  - **UPDATE**: Modify an existing record  
  - **DELETE**: Remove a record

- **Simple JOIN Support**  
  - **INNER JOIN**: Combine data from two tables based on a matching key.
  - **LEFT JOIN**: Retrieve all records from the left table and matching records from the right table.

- **Persistence**  
  Data is stored in a file-based format (JSON, or you can customize it to a binary format, SQLite, etc.).

- **Indexing**  
  An in-memory index can be used for faster lookups and JOINs on large datasets.

---


